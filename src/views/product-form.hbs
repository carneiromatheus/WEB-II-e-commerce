<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Novo Produto</title>
</head>
<body>
    <h1>Cadastrar Produto</h1>
    
    {{#if error}}
        <p style="color: red;">{{error}}</p>
    {{/if}}

    <form action="/seller/new" method="POST">
        <div style="margin-bottom: 10px;">
            <label>Nome do Produto:</label><br>
            <input type="text" name="name" required style="width: 300px;">
        </div>
        
        <div style="margin-bottom: 10px;">
            <label>Descrição:</label><br>
            <textarea name="description" rows="4" style="width: 300px;"></textarea>
        </div>
        
        <div style="margin-bottom: 10px;">
            <label>Preço (R$):</label><br>
            <input type="number" name="price" step="0.01" required>
        </div>

        <div style="margin-bottom: 10px; border: 1px solid #ccc; padding: 10px; width: 300px;">
            <label><strong>Categorias:</strong></label><br>
            
            {{#each categories}}
                <div style="margin-top: 5px;">
                    <input type="checkbox" name="categories" value="{{this.id}}" id="cat-{{this.id}}">
                    <label for="cat-{{this.id}}">
                        {{this.name}} 
                        {{#if this.parent_name}}<small>({{this.parent_name}})</small>{{/if}}
                    </label>
                </div>
            {{else}}
                <small>Nenhuma categoria cadastrada.</small>
            {{/each}}
        </div>
        
        <div style="margin-bottom: 10px;">
            <label>URL da Imagem:</label><br>
            <input type="text" name="photo_url" placeholder="https://..." style="width: 300px;">
        </div>

        <button type="submit" style="padding: 10px 20px;">Salvar Produto</button>
        <a href="/seller/dashboard" style="margin-left: 10px;">Cancelar</a>
    </form>
</body>
</html>